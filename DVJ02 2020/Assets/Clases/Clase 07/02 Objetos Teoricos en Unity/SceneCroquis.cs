using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace DVJ02.Clase07
{
//Teorica implementacion de la scene de Unity
    public class SceneCroquis
    {
        private List<GameObject> gos;

        public void AlDarlePlayAlEditor()
        {
            //for( todos los GameObject en la escena)
            //  go = new GameObject(GOdata) //data es la informacion que tiene el editor sobre los valores iniciales del GO, seteados en el editor.
            //  gos.Add(go)
        }

	    void MailLoop()
	    {
		    //    for (int i = 0; i < gos.Count; i++)
		    //    {
		    //	    gos[i].Start()
		    //    }

			//while (true)
			//{
			//    for (int i = 0; i < gos.Count; i++)
			//    {
			//	    gos[i].Update()
			//    }
			//}
		}
	}
}
